<Page
    x:Class="MyMemories.TreeViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="200" MaxWidth="500"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Tree View -->
        <Grid Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tree Header -->
            <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock 
                        Text="My Memories" 
                        FontSize="18" 
                        FontWeight="SemiBold"/>
                    <AutoSuggestBox 
                        x:Name="SearchBox"
                        PlaceholderText="Search items..."
                        QuerySubmitted="SearchBox_QuerySubmitted"/>
                </StackPanel>
            </Border>

            <!-- Tree View -->
            <ScrollViewer Grid.Row="1" Padding="8">
                <TreeView 
                    x:Name="ItemsTreeView"
                    SelectionMode="Single"
                    ItemInvoked="ItemsTreeView_ItemInvoked">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem 
                                ItemsSource="{Binding Children}"
                                IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon 
                                        Glyph="{Binding Icon}" 
                                        FontSize="16"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                    <TextBlock 
                                        Text="{Binding Name}" 
                                        VerticalAlignment="Center"/>
                                    <TextBlock 
                                        Text="{Binding Count}" 
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        FontSize="12"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding ShowCount}"/>
                                </StackPanel>
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </ScrollViewer>

            <!-- Tree Footer with Actions -->
            <Border Grid.Row="2" Padding="12,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <Button 
                        Content="Add Category" 
                        Click="AddCategory_Click"
                        HorizontalAlignment="Stretch"/>
                    <Button 
                        Content="Import Files" 
                        Click="ImportFiles_Click"
                        HorizontalAlignment="Stretch"
                        Style="{ThemeResource AccentButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Splitter -->
        <Border 
            Grid.Column="1" 
            Width="1" 
            Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

        <!-- Right Panel: Detail View -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Detail Header -->
            <Border Grid.Row="0" Padding="16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock 
                            x:Name="DetailTitle" 
                            Text="Select an item" 
                            FontSize="20" 
                            FontWeight="SemiBold"/>
                        <TextBlock 
                            x:Name="DetailSubtitle" 
                            Text="" 
                            FontSize="12" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <CommandBar Grid.Column="1" DefaultLabelPosition="Right">
                        <AppBarButton Icon="Edit" Label="Edit" Click="EditItem_Click"/>
                        <AppBarButton Icon="Delete" Label="Delete" Click="DeleteItem_Click"/>
                        <AppBarButton Icon="Share" Label="Share" Click="ShareItem_Click"/>
                    </CommandBar>
                </Grid>
            </Border>

            <!-- Detail Content -->
            <ScrollViewer Grid.Row="1" Padding="16">
                <StackPanel x:Name="DetailPanel" Spacing="16">
                    <!-- Empty State -->
                    <StackPanel 
                        x:Name="EmptyState"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Margin="0,80,0,0">
                        <FontIcon 
                            Glyph="&#xE8F1;" 
                            FontSize="64"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        <TextBlock 
                            Text="No item selected" 
                            FontSize="16"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            HorizontalAlignment="Center"/>
                        <TextBlock 
                            Text="Select an item from the tree to view its details" 
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            HorizontalAlignment="Center"
                            TextWrapping="Wrap"
                            TextAlignment="Center"
                            MaxWidth="300"/>
                    </StackPanel>

                    <!-- Detail Content (Hidden by default) -->
                    <StackPanel x:Name="DetailContent" Visibility="Collapsed" Spacing="24">
                        <!-- Image Preview -->
                        <Border 
                            x:Name="ImagePreview"
                            Visibility="Collapsed"
                            CornerRadius="8"
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            Padding="16">
                            <Image 
                                x:Name="PreviewImage"
                                MaxHeight="400"
                                Stretch="Uniform"
                                HorizontalAlignment="Center"/>
                        </Border>

                        <!-- Properties Section -->
                        <StackPanel Spacing="12">
                            <TextBlock 
                                Text="Properties" 
                                FontSize="16" 
                                FontWeight="SemiBold"/>

                            <Grid RowSpacing="8" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Type:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TypeText" Text=""/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Created:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="CreatedText" Text=""/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Modified:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="ModifiedText" Text=""/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Size:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="SizeText" Text=""/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Location:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="LocationText" Text="" TextWrapping="Wrap"/>
                            </Grid>
                        </StackPanel>

                        <!-- Description Section -->
                        <StackPanel Spacing="12">
                            <TextBlock 
                                Text="Description" 
                                FontSize="16" 
                                FontWeight="SemiBold"/>
                            <TextBox 
                                x:Name="DescriptionBox"
                                PlaceholderText="Add a description..."
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                MinHeight="100"
                                MaxHeight="200"/>
                        </StackPanel>

                        <!-- Tags Section -->
                        <StackPanel Spacing="12">
                            <TextBlock 
                                Text="Tags" 
                                FontSize="16" 
                                FontWeight="SemiBold"/>
                            <ItemsControl x:Name="TagsPanel">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="8"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border 
                                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                                            CornerRadius="12"
                                            Padding="12,4">
                                            <TextBlock Text="{Binding}" FontSize="12"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="Add Tag" Click="AddTag_Click"/>
                        </StackPanel>

                        <!-- Related Items Section -->
                        <StackPanel Spacing="12">
                            <TextBlock 
                                Text="Related Items" 
                                FontSize="16" 
                                FontWeight="SemiBold"/>
                            <ListView 
                                x:Name="RelatedItemsList"
                                SelectionMode="None"
                                MaxHeight="200">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="8" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Grid.Column="0" Glyph="{Binding Icon}" FontSize="16"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>